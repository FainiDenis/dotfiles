# git completion is provided via fpath + compinit.
# Do not source Homebrew's `_git` directly: it is an autoloaded completion
# function file and executes completion internals only in completion context.
